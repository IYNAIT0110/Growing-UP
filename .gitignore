# 敏感凭据（绝不能上传）
douyin_cookies.txt

# 去重状态（本机私有，不上传）
state.json

# 大文件 / 媒体
downloads/
videos/

# 转写中间产物
transcripts/

# 工具脚本
*.py
__pycache__/

# 音频
*.mp3

# 词典缓存（构建期用，删了会自动重抓，不上传）
wordcache/

# 其他
*.log
.DS_Store

# 单次运行的临时输出（供人工排查，不入库）
run_out.json
retry_out.json
probe_out.txt

# 本机记忆/自动化运行记录（私有，不入库）
.workbuddy/
